= JBANG-COMPLETION

== NAME

jbang completion -- Generate bash/zsh or fish completion script for jbang.

== SYNOPSIS

[source]
----
jbang completion [-hox] [-s=<shell>] [--config=<config>] [--insecure] [--verbose] [--quiet] [--fresh]
----

== DESCRIPTION

Generate bash/zsh or fish completion script for jbang.

== OPTIONS

*-h*, *--help*::
Display this help and exit

*-s*, *--shell*=<shell>::
The shell to generate the completion script for. Supported shells: bash, zsh, and fish

*--config*=<config>::
Path to config file to be used instead of the default

*--insecure*::
Enable insecure trust of all SSL certificates.

*--verbose*::
jbang will be verbose on what it does.

*--quiet*::
jbang will be quiet, only print when error occurs.

*-o*, *--offline*::
Work offline. Fail-fast if dependencies are missing.

*--fresh*::
Make sure we use fresh (i.e. non-cached) resources.

*-x*, *--stacktrace*::
Print exceptions stacktraces to stderr (even when quiet).

